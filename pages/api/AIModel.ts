// import tensorflow and keras

const model = null; //load the model

const predict = (img: any) : string => {
  // process image
  // predict image
  return "A espera de implementación"// return image
}
export {model, predict}